import { ArrowLeft, ArrowRight, CalendarDays, CheckCircle2, Clock3, Handshake, MapPin, MessageCircle, Share2, SlidersHorizontal, UsersRound } from 'lucide-react'
import { useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { collaborationData } from '../data/collaborationData'
import { Button } from '../components/ui'

function StatusBadge({ status, tone }) { return <span className={`collaboration-status ${tone}`}><span />{status}</span> }

export default function CollaborationDetail() {
  const { projectId } = useParams()
  const project = collaborationData.find((item) => item.id === projectId)
  const [interested, setInterested] = useState(false)
  if (!project) return <div className="collaboration-detail"><Link to="/collaboration" className="back-link"><ArrowLeft size={16} /> Back to collaborations</Link><div className="collaboration-empty"><div><Handshake size={23} /></div><h2>Project not found</h2><p>This collaboration may have closed or moved.</p><Link className="button button-secondary" to="/collaboration">Explore collaborations</Link></div></div>
  return <div className="collaboration-detail"><Link to="/collaboration" className="back-link"><ArrowLeft size={16} /> Back to collaborations</Link><div className="collaboration-detail-layout"><main><header className="collaboration-detail-hero"><div className={`collaboration-detail-logo ${project.tone}`}>{project.initials}</div><div><span className="eyebrow">{project.partnerType}</span><h1>{project.title}</h1><p>{project.organization}</p><div className="collaboration-detail-tags"><StatusBadge status={project.status} tone={project.statusTone} /><span><Handshake size={14} />{project.institution}</span><span><MapPin size={14} />{project.location}</span></div></div><button className="collaboration-share" aria-label="Share project"><Share2 size={17} /></button></header><section className="collaboration-detail-section"><span className="eyebrow">Project description</span><h2>Learn, contribute and make an impact.</h2><p>{project.description}</p></section><section className="collaboration-detail-section"><span className="eyebrow">What the collaboration will create</span><ul className="collaboration-outcomes">{project.outcomes.map((outcome) => <li key={outcome}><CheckCircle2 size={16} />{outcome}</li>)}</ul></section><section className="collaboration-detail-section"><span className="eyebrow">How it works</span><div className="collaboration-steps"><div><b>01</b><strong>Align</strong><span>Meet the partner team and agree on a focused question.</span></div><div><b>02</b><strong>Make</strong><span>Work in a small student and mentor team over the project period.</span></div><div><b>03</b><strong>Share</strong><span>Present the outcome to the academic and industry community.</span></div></div></section></main><aside className="collaboration-detail-sidebar"><div className="collaboration-interest-card"><span className="eyebrow">Interested in joining?</span><h2>{interested ? 'Interest registered.' : 'Bring your perspective.'}</h2><p>{interested ? 'The project lead will be in touch with next steps.' : 'Share your interest and the project team can follow up with the right context.'}</p><Button icon={interested ? CheckCircle2 : ArrowRight} onClick={() => setInterested(true)}>{interested ? 'Interest registered' : 'Express interest'}</Button><button className="button button-secondary collaboration-join-button" onClick={() => setInterested(true)}><UsersRound size={17} /> Join collaboration</button><span className="collaboration-note">Mock collaboration flow · no backend connection</span></div><div className="collaboration-detail-facts"><div><UsersRound size={16} /><span>Team size<strong>{project.teamSize}</strong></span></div><div><Clock3 size={16} /><span>Duration<strong>{project.duration}</strong></span></div><div><CalendarDays size={16} /><span>Next milestone<strong>{project.nextMilestone}</strong></span></div><div><SlidersHorizontal size={16} /><span>Required skills<strong>{project.skills.join(' · ')}</strong></span></div></div><div className="collaboration-skills"><span className="eyebrow">Skills you will use</span><div>{project.skills.map((skill) => <span key={skill}>{skill}</span>)}</div></div><button className="collaboration-contact"><MessageCircle size={15} /> Contact project lead</button></aside></div></div>
}
