import { ArrowLeft, ArrowRight } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { useAuth } from '../context/AuthContext'

export default function Login() { const [role, setRole] = useState('student'); const { signIn } = useAuth(); const navigate = useNavigate(); const submit = async (event) => { event.preventDefault(); await signIn(role, role === 'student' ? 'Aisha Khan' : role === 'industry' ? 'Jordan Lee' : 'Dr. Maya Patel'); navigate(`/${role}`) }; return <div className="auth-page"><Link to="/" className="brand auth-brand"><span className="brand-mark">B</span><span>bridgework</span></Link><div className="auth-content compact-auth"><Link to="/" className="back-link"><ArrowLeft size={16} /> Back to home</Link><span className="eyebrow accent-eyebrow">Welcome back</span><h1>Sign in to your workspace.</h1><p className="auth-lead">This prototype uses mock authentication for development.</p><form className="auth-form" onSubmit={submit}><label>Workspace role<select value={role} onChange={(event) => setRole(event.target.value)}><option value="student">Student</option><option value="industry">Industry professional</option><option value="academia">Academic</option></select></label><label>Work email<input type="email" placeholder="you@institution.edu" required /></label><button className="button button-primary" type="submit">Continue to workspace <ArrowRight size={17} /></button></form><small className="dev-note">No password is stored or checked in this frontend prototype.</small></div></div> }
